---
title: createPolymorphicComponent
description: Create a polymorphic component with asChild support
category: reference
tags: [functions, api]
---

# createPolymorphicComponent

Create a polymorphic component with asChild support

## Parameters

### defaultElement

**Type**: `T`

**Required**

No description available



## Returns

`<C extends React.ElementType = T>({ as, asChild, ...props }: { as?: C | undefined; } & React.PropsWithoutRef<React.ComponentProps<C>> & import("C:/AI-BOS/mythic/packages/shared-utils/src/composition").AsChildProps) => React.ReactElement<any, string | React.JSXElementConstructor<any>>`



## Examples

```typescript
```tsx
const Button = forwardRef<HTMLButtonElement, ButtonProps>(
  ({ asChild, className, ...props }, ref) => {
    const Comp = asChild ? Slot : 'button'
    return <Comp ref={ref} className={cn(buttonVariants(), className)} {...props} />
  }
)
```
```


