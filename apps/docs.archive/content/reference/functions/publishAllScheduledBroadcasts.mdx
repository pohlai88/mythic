---
title: publishAllScheduledBroadcasts
description: Publish all ready scheduled broadcasts

Automatically publishes all broadcasts whose scheduledFor time has passed.
This should be called periodically (e.g., via cron job).
category: reference
tags: [functions, api]
---

# publishAllScheduledBroadcasts

Publish all ready scheduled broadcasts

Automatically publishes all broadcasts whose scheduledFor time has passed.
This should be called periodically (e.g., via cron job).



## Returns

`Promise<{ published: number; errors: number; }>`




